version: "3"
services:
  nexus:
    image: sonatype/nexus3
    container_name: nexus
    ports:
      - "8081:8081"
    volumes:
      - nexus-data:/nexus-data
    restart: unless-stopped
    user: "200:200" # Ensures correct UID/GID for /nexus-data
volumes:
  nexus-data:
    driver: local
